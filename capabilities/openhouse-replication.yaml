apiVersion: naftiko.dev/v1
kind: Capability
metadata:
  name: mlsgrid-openhouse-replication
  title: Replicate MLS Grid OpenHouse events
  description: Incrementally replicate OpenHouse events from one or more MLS Grid originating systems.
  provider: mlsgrid
  api: mlsgrid-reso-web-api
spec:
  binding:
    method: GET
    url: https://api.mlsgrid.com/v2/OpenHouse
    auth: bearer
  inputs:
  - name: originatingSystemName
    type: string
    required: true
  - name: modifiedSince
    type: string
    format: date-time
    required: true
  query:
    $filter: OriginatingSystemName eq '{originatingSystemName}' and ModificationTimestamp gt {modifiedSince}
  pagination:
    type: odata-next-link
    nextField: '@odata.nextLink'
  rateLimits:
    rps: 2
    perHour: 7200
    perDay: 40000
